---
title: pelican transfer credential
---

## pelican transfer credential

Manage transfer credentials

### Options

```
  -h, --help   help for credential
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
  -s, --server string       URL of the transfer server (e.g. https://transfer.example.com:8447)
  -t, --token string        Path to a token file for authentication
      --version             Print the version and exit
```

### SEE ALSO

* [pelican transfer](/commands-reference/transfer/)	 - Manage the transfer server and its resources
* [pelican transfer credential add](/commands-reference/transfer/credential/add/)	 - Add a new credential to the transfer server
* [pelican transfer credential delete](/commands-reference/transfer/credential/delete/)	 - Delete a credential from the transfer server
* [pelican transfer credential list](/commands-reference/transfer/credential/list/)	 - List credentials on the transfer server
