---
title: pelican credentials prefix add
---

## pelican credentials prefix add

Add a new oauth client

### Synopsis

Add a new oauth client

```
pelican credentials prefix add <prefix> [flags]
```

### Options

```
  -h, --help   help for add
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug logs
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican credentials prefix](/commands-reference/credentials/prefix/)	 - Manage the prefix configuration
