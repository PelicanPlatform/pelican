---
title: pelican origin collection metadata get
---

## pelican origin collection metadata get

Get all metadata for a collection

```
pelican origin collection metadata get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican origin collection metadata](/commands-reference/origin/collection/metadata/)	 - Manage collection metadata
