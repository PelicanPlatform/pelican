---
title: pelican origin issuer
---

## pelican origin issuer

Manage the origin's embedded OIDC token issuer

### Options

```
  -h, --help   help for issuer
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican origin](/commands-reference/origin/)	 - Operate a Pelican origin service
* [pelican origin issuer client](/commands-reference/origin/issuer/client/)	 - Manage OIDC clients for the origin's embedded issuer
