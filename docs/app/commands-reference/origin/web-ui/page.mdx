---
title: pelican origin web ui
---

## pelican origin web-ui

Manage the Pelican origin web UI

### Options

```
  -h, --help   help for web-ui
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug logs
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican origin](/commands-reference/origin/)	 - Operate a Pelican origin service
* [pelican origin web-ui reset-password](/commands-reference/origin/web-ui/reset-password/)	 - Reset the admin password for the web UI
