---
title: pelican origin user set status
---

## pelican origin user set-status

Update user status and display name

```
pelican origin user set-status <user-id> [flags]
```

### Options

```
      --display-name string   Display name
  -h, --help                  help for set-status
      --status string         Status: 'active' or 'inactive'
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican origin user](/commands-reference/origin/user/)	 - Manage users on a Pelican origin
