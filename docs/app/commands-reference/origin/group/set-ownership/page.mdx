---
title: pelican origin group set ownership
---

## pelican origin group set-ownership

Set owner or admin of a group

```
pelican origin group set-ownership <group-id> [flags]
```

### Options

```
      --admin-id string     Admin user or group ID
      --admin-type string   Admin type: 'user' or 'group'
  -h, --help                help for set-ownership
      --owner-id string     New owner user ID
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican origin group](/commands-reference/origin/group/)	 - Manage groups on a Pelican origin
