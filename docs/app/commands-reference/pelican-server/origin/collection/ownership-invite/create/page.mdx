---
title: pelican server origin collection ownership invite create
---

## pelican-server origin collection ownership-invite create

Mint an ownership-transfer invite for a collection

```
pelican-server origin collection ownership-invite create <id> [flags]
```

### Options

```
      --expires-in string   Expiration duration as a Go duration string (e.g. '168h'); defaults to 7 days server-side
  -h, --help                help for create
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican-server origin collection ownership-invite](/commands-reference/pelican-server/origin/collection/ownership-invite/)	 - Manage collection ownership-transfer invite links
