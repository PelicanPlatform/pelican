---
title: pelican server server user identity add
---

## pelican-server server user identity add

Add an identity to a user

```
pelican-server server user identity add <user-id> [flags]
```

### Options

```
  -h, --help            help for add
      --issuer string   OIDC issuer URL (required)
      --sub string      OIDC subject claim (required)
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican-server server user identity](/commands-reference/pelican-server/server/user/identity/)	 - Manage user identities
