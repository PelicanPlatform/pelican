---
title: pelican server server user scope revoke
---

## pelican-server server user scope revoke

Revoke a directly-granted scope from a user

```
pelican-server server user scope revoke <user-id> <scope> [flags]
```

### Options

```
  -h, --help   help for revoke
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican-server server user scope](/commands-reference/pelican-server/server/user/scope/)	 - Manage scope grants on a user
