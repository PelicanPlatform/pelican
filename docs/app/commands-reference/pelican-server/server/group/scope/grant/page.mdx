---
title: pelican server server group scope grant
---

## pelican-server server group scope grant

Grant a scope to a group

```
pelican-server server group scope grant <group-id> [flags]
```

### Options

```
  -h, --help           help for grant
      --scope string   Scope to grant (e.g. 'server.user_admin'); must be in token_scopes.UserGrantableScopes
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican-server server group scope](/commands-reference/pelican-server/server/group/scope/)	 - Manage scope grants on a group
