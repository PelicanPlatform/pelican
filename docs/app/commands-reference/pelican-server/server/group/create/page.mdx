---
title: pelican server server group create
---

## pelican-server server group create

Create a new group

```
pelican-server server group create [flags]
```

### Options

```
      --description string   Description of the group
  -h, --help                 help for create
      --name string          Name of the group (required)
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican-server server group](/commands-reference/pelican-server/server/group/)	 - Manage groups on a Pelican server
