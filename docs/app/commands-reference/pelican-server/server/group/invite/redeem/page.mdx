---
title: pelican server server group invite redeem
---

## pelican-server server group invite redeem

Redeem an invite link token to join a group

```
pelican-server server group invite redeem <token> [flags]
```

### Options

```
  -h, --help   help for redeem
```

### Options inherited from parent commands

```
      --config string       config file (default is $HOME/.config/pelican/pelican.yaml)
  -d, --debug               Enable debug log messages
  -f, --federation string   Pelican federation to utilize
      --json                output results in JSON format
  -L, --log string          Specified log output file
      --version             Print the version and exit
```

### SEE ALSO

* [pelican-server server group invite](/commands-reference/pelican-server/server/group/invite/)	 - Manage group invite links
