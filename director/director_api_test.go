package director

import "testing"

// TODO: test listnamespaces by first register
// a new origin and evoke listnamespaces to see
// if it returns the namespace registered
// other cases includes empty nss, two nss from same origin,
// nss from different origins
func TestListNamespaces(t *testing.T) {
	
}