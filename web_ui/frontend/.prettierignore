# The `format` scripts pass `--ignore-path .gitignore --ignore-path .prettierignore`,
# because a bare `--ignore-path .gitignore` replaces this file rather than adding
# to it. Entries here are for committed files Prettier should not reformat.

# Generated by pnpm. Reformatting it produces a diff on every install and can
# make the lockfile look hand-edited in review.
pnpm-lock.yaml
